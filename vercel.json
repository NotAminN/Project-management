{
  "buildCommand": "(python backend/manage.py migrate --noinput && python backend/manage.py collectstatic --noinput) || (python3 backend/manage.py migrate --noinput && python3 backend/manage.py collectstatic --noinput); cd front && npm install && npm run build",
  "outputDirectory": "front/dist",
  "functions": {
    "api/index.py": {
      "includeFiles": "backend/**"
    }
  }
}